{
  "name": "exercise-eval",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Browser-based evaluation of exercise in type 1 diabetes. Development tooling only; the published site loads vendored libraries and has no runtime npm dependency.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "vendor": "node tools/vendor.js",
    "fixtures": "python3 tools/make_fixture.py && python3 tools/make_fit_fixture.py && python3 tools/make_strava_fixture.py",
    "check": "npm run fixtures && npm test && python3 -m pytest tests/ -q && python3 tools/mutation_check.py"
  },
  "devDependencies": {
    "@xmldom/xmldom": "^0.9.12",
    "fflate": "0.8.2",
    "sql.js": "1.13.0"
  }
}
